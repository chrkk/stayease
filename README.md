# StayEase
Boarding house & condo management system.

## Setup
1. Import `database/stayeasedb.sql` into MySQL
2. Copy `config.example.php` to `config.php` and fill in DB credentials
3. Point your web server root to `public/`

## Default login
Username: `admin` | Password: `1234`
